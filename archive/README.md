# Archive

This folder stores older versions of project files for reference. Nothing here
is referenced by the live report (`../index.html`).

## Contents

- `project2_checkpoint_v1.pdf` — checkpoint submission PDF (Apr 26).
- `superseded_v2/` — earlier drafts replaced by the final submission:
  - `project2_report.html` — earlier HTML report; replaced by `../index.html`.
  - `make_charts.py` — earlier chart-generation script; replaced by `../generate_charts.py`.
  - `index_fixed.html`, `generate_charts_fixed.py`, `chart3_fixed.png` —
    intermediate "fix" versions whose changes were merged into the root files.
  - `test_img.png` — placeholder image used while wiring up the report; no
    longer referenced.
